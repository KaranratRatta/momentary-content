# ─── Dependencies ───────────────────────────────────────────────────────────
# Install with: pip install -r requirements.txt

# Core
pyyaml>=6.0
Pillow>=10.0
python-dotenv>=1.0       # loads API keys from .env

# LLM prompt generation (OpenRouter is OpenAI-compatible)
openai>=1.0

# Video assembly (future — uncomment when ready)
# moviepy>=1.0.3

# Audio / TTS (future — uncomment when ready)
# edge-tts>=6.0

# Development
pytest>=7.0